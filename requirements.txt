# Зависимости повторяют боевые зависимости библиотеки avito: она лежит рядом клоном
# (lib/avito_library), пакета в PyPI нет, поэтому её требования ставим сами.
#
# curl_cffi — единственный клиент, чей отпечаток TLS Авито пропускает.
# opencv и numpy нужны решателю капчи, pycryptodome — ему же.

curl_cffi==0.15.0
asyncpg==0.31.0
opencv-python-headless==4.13.0.92
numpy>=2.3
pycryptodome==3.23.0
